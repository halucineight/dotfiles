
# yazi.toml
[mgr]
show_hidden = false

[opener]
edit = [
  { run = "nvim %s", block = true, for = "unix" },
]
eog = [
{run = 'eog "$@"', orphan = true, for = "unix"},
]
vlc = [
{run = 'vlc "$@"', orphan = true, for = "unix"},
]

[open]
prepend_rules = [
    { mime = "image/*", use = [ "eog", "reveal" ] },
    { mime = "video/*", use = [ "vlc", "reveal" ] }
]

